camino_nodes:
  hosts:
    must-be-specified.compute.amazonaws.com:
    must-be-specified.compute.amazonaws.com:
  vars:
    ansible_ssh_private_key_file: "~/.ssh/aws_identity.pem"
    ansible_user: "camino"
