apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bootstrap-tester-testnet-state-sync
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ebs-fast
