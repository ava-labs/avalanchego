apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bootstrap-tester-testnet-full-sync
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1000Gi
  storageClassName: ebs-fast
