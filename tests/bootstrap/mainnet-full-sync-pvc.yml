apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bootstrap-tester-mainnet-full-sync
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2000Gi
  storageClassName: ebs-fast
