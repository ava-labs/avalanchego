name: build-deb-packages

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build DEBs for'
        required: true
  push:
    tags:
      - "v*"
  pull_request:
    paths:
      - ".github/packaging/**"
      - ".github/workflows/build-deb-release.yml"

jobs:
  build-deb:
    strategy:
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-22.04
            deb_arch: amd64
          - arch: arm64
            runner: custom-arm64-jammy
            deb_arch: arm64
    runs-on: ${{ matrix.runner }}
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - uses: ./.github/actions/setup-packaging
        id: setup
        with:
          tag-input: ${{ github.event.inputs.tag }}
          gpg-private-key: ${{ github.event_name != 'pull_request' && secrets.RPM_GPG_PRIVATE_KEY || '' }}

      - name: Build and validate DEBs
        run: ./scripts/run_task.sh --taskfile .github/packaging/Taskfile.yml test-build-debs
        env:
          PACKAGING_TAG: ${{ steps.setup.outputs.tag }}
          DEB_GPG_KEY_FILE: ${{ steps.setup.outputs.gpg-key-file }}
          NFPM_DEB_PASSPHRASE: ${{ secrets.RPM_GPG_PASSPHRASE }}

      - name: Upload DEBs as artifacts
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: debs-${{ matrix.deb_arch }}
          path: |
            build/deb/avalanchego-${{ steps.setup.outputs.tag }}-${{ matrix.deb_arch }}.deb
            build/deb/subnet-evm-${{ steps.setup.outputs.tag }}-${{ matrix.deb_arch }}.deb
            build/deb/DEB-GPG-KEY-avalanchego

      - name: Configure AWS credentials
        if: (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')) || github.event_name == 'workflow_dispatch'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_SA_ROLE_ARN }}
          role-session-name: githubrolesession
          aws-region: us-east-1

      - name: Install aws cli
        if: (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')) || github.event_name == 'workflow_dispatch'
        run: |
          if ! command -v aws &>/dev/null; then
            sudo snap install aws-cli --classic
          fi

      - name: Upload DEBs to S3
        if: (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')) || github.event_name == 'workflow_dispatch'
        run: |
          for release in jammy noble; do
            aws s3 cp "build/deb/avalanchego-${{ steps.setup.outputs.tag }}-${{ matrix.deb_arch }}.deb" \
              "s3://${{ secrets.BUCKET }}/linux/debs/ubuntu/${release}/${{ matrix.deb_arch }}/"
            aws s3 cp "build/deb/subnet-evm-${{ steps.setup.outputs.tag }}-${{ matrix.deb_arch }}.deb" \
              "s3://${{ secrets.BUCKET }}/linux/debs/ubuntu/${release}/${{ matrix.deb_arch }}/"
            aws s3 cp "build/deb/DEB-GPG-KEY-avalanchego" \
              "s3://${{ secrets.BUCKET }}/linux/debs/ubuntu/${release}/"
          done

      - name: Cleanup
        if: always()
        run: |
          if [[ -n "${{ steps.setup.outputs.gpg-key-file }}" ]]; then
            rm -f "${{ steps.setup.outputs.gpg-key-file }}"
          fi
          sudo rm -rf build/deb build/gpg
        shell: bash
