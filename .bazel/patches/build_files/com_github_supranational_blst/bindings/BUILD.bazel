# gazelle:ignore
# Header library for blst C bindings

load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "bindings",
    hdrs = glob(["*.h"]),
    includes = ["."],
    visibility = ["//:__subpackages__"],
)
