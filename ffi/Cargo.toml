[package]
name = "firewood-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
libc = "0.2.2"
firewood = { path = "../firewood" }

[build-dependencies]
cbindgen = "0.28.0"
