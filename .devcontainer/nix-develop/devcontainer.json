{
  "name": "AvalancheGo (nix develop)",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "../.."
  },
  "initializeCommand": ["sh", "-c", "mkdir -p tmp && git bundle create tmp/.git-bundle HEAD \"$(git symbolic-ref --short HEAD)\" && git config --local --list | grep '^remote\\.' > tmp/.git-remotes"],
  "workspaceMount": "",
  "workspaceFolder": "/workspace/avalanchego",
  "remoteUser": "root"
}
