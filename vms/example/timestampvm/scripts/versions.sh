#!/usr/bin/env bash

# Don't export them as they're used in the context of other calls
avalanche_version=${AVALANCHE_VERSION:-'v1.10.2'}

avalanche_network_runner_version=${AVALANCHE_NETWORK_RUNNER_VERSION:-'v1.6.0'}
